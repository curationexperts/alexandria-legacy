<% if LocalAuthority.local_name_models.include?(@record.class) %>
  <%= render partial: 'records/agent_form' %>
<% elsif LocalAuthority.local_subject_models.include?(@record.class) %>
  <%= render partial: 'records/topic_form' %>
<% else %>
  <%= render partial: 'records/form' %>
<% end %>
