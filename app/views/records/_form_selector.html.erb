<% if LocalAuthority.local_name_authority?(@record) %>
  <%= render partial: 'records/agent_form' %>
<% elsif LocalAuthority.local_subject_authority?(@record) %>
  <%= render partial: 'records/topic_form' %>
<% else %>
  <%= render partial: 'records/form' %>
<% end %>
